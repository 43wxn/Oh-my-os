# OS 内核实现（难度系数 100%）

## 一、总体思路

通过从零开始实现一个简化版操作系统内核，深入理解操作系统底层原理，掌握系统启动、内存管理、进程调度、中断处理、文件系统等核心机制，培养系统级编程能力和工程实践能力。

以**程序加载 + 系统启动 + 内核管理 + 文件系统**为核心，面向"系统开发者"视角。**最终目标：在真实老笔记本上原生运行**，直接通过 U 盘/硬盘在物理机上引导运行。

**开发方式可选（按需选择，不强制）：**
- **直接实机开发**：在开发机上写代码 → 编译构建 → 制作可启动 U 盘 → 插入老笔记本重启验证。适合"一次写对"型开发，优点是驱动代码一步到位，无需后续移植。
- **QEMU 辅助开发**：在开发机上用 QEMU/Bochs 快速迭代调试（2 秒启动、GDB 远程断点），阶段性在实机上验证。适合需要频繁单步调试的阶段。

### 目标硬件规格（实机实测数据）

| 项目 | 规格 |
|------|------|
| **型号** | 华硕 VivoBook 15 X542UF（ASUSTeK COMPUTER INC.）|
| **CPU** | Intel Core i5-8250U @ 1.60GHz（Turbo 3.40GHz），4 核 8 线程 |
| **架构** | x86_64，支持 32 位/64 位模式，物理地址 39 位，虚拟地址 48 位 |
| **特性** | PAE ✓、VT-x ✓、SMEP ✓、SMAP ✓、NX ✓ |
| **内存** | 8GB（实际可用 ~7.7GB） |
| **硬盘** | 111.8GB SATA SSD，**AHCI 模式**（Intel Sunrise Point-LP SATA Controller） |
| **显卡** | Intel UHD Graphics 620（集成）+ NVIDIA GeForce MX130（独立） |
| **固件** | **Legacy BIOS**（非 UEFI） |
| **键盘** | **PS/2**（AT Translated Set 2，端口 0x60/0x64，IRQ 1） |
| **显示输出** | eDP 内建显示器（通过 Intel 集显输出） |

**⚠️ 关键结论：**
- **磁盘接口是 SATA AHCI，不是 IDE PIO**——需要实现 AHCI 驱动，复杂度显著高于 IDE
- **BIOS 而非 UEFI**——可以用 MBR 启动扇区方案，bootloader 设计简化
- **PS/2 键盘真实存在**——键盘驱动直接用端口 I/O，无需 USB HID 栈
- **内存充足**——8GB 远超需求，可以忽略内存紧张场景，简化分配器设计

## 二、项目技术方案

### 1. 系统启动（Bootloader）

**功能示例：**

- 实现 BIOS 启动扇区（MBR，512 字节），支持从硬盘或 U 盘引导
- 实现从实模式到保护模式（x86）的切换
- 通过 BIOS E820 中断探测物理内存布局，记录可用内存区域
- 加载内核镜像从磁盘到内存指定位置并跳转执行
- 初始化段描述符表（GDT/IDT）
- 设置栈指针，为 C 语言运行环境做准备
- 输出启动日志信息到 VGA 文本模式显存，证明启动成功
- 支持 Multiboot 规范，兼容 GRUB 引导（双引导方案）

**技术指标：**

- 能在 QEMU 中成功启动并进入内核主函数
- **最终能在实机（老笔记本）上通过 U 盘或硬盘成功启动**
- 启动扇区能正确读取磁盘上的内核镜像（FAT32 文件系统或裸扇区）
- 启动过程稳定可复现，无异常重启或死机
- bootloader 在两种引导方式下均工作：自定义 MBR + GRUB fallback

### 2. 中断与异常处理

**功能示例：**

- 实现中断描述符表（IDT）的初始化与注册
- 编程 8259A PIC（可编程中断控制器），实现硬件中断管理
- 实现时钟中断处理程序（PIT 8253/8254），支持定时功能
- 实现键盘中断处理（PS/2 键盘，端口 0x60/0x64），支持基本输入
- 实现系统调用接口（int 0x80），支持用户态到内核态切换
- 实现缺页异常处理
- 实现双重错误（Double Fault）处理，防止内核崩溃

**技术指标：**

- 时钟中断频率稳定（如 100Hz），在实机上频率误差 <5%
- 键盘中断能正确读取 PS/2 扫描码并转换为 ASCII 字符
- 系统调用支持：打印、进程创建、进程退出、文件读写
- 缺页异常能正确处理 Demand Paging 和非法访问

### 3. 内存管理

**功能示例：**

- 通过 BIOS E820 实现物理内存的探测与管理（识别可用/保留/ACPI 区域）
- 实现物理页框分配器（基于位图的伙伴算法）
- 实现虚拟内存管理：页表（二级页表）创建、映射、解除映射
- 实现内核堆内存分配（kmalloc/kfree，支持多种大小分配）
- 实现用户态内存空间布局（代码段、数据段、堆、栈）
- 支持内存映射 I/O 区域保护

**技术指标：**

- 支持至少 64MB 物理内存管理（实机实际可用内存）
- 页大小为 4KB
- 支持按需分页（Demand Paging）
- 内存分配无泄漏，支持释放后重用
- 物理内存信息和页框分配状态在 VGA 上可视化输出

### 4. 进程管理

**功能示例：**

- 实现进程控制块（PCB）数据结构
- 实现进程创建（fork/exec）
- 实现进程调度器（支持至少两种算法：FCFS 和 RR）
- 实现进程状态转换：就绪、运行、阻塞、僵尸
- 实现进程间同步机制：信号量、互斥锁
- 实现进程退出与等待（wait/waitpid）
- 实现简单的信号机制（SIGKILL、SIGTERM）

**技术指标：**

- 支持多个并发进程（≥8 个）
- 时间片轮转调度时间片可配置（默认 10ms）
- 进程切换开销小于 1ms（在实机上）
- 支持父子进程关系树（ps 命令可视化）

### 5. 文件系统

**功能示例：**

- 实现简化版文件系统（基于内存的 RAMFS 用于开发 + FAT32 用于实机磁盘读写）
- 支持文件/目录的创建、删除、打开、关闭、读写
- 实现文件描述符表管理
- 实现路径解析（绝对路径与相对路径）
- 支持标准输入输出重定向
- **实机上支持从磁盘（ATA PIO 模式）读取/写入文件**

**技术指标：**

- RAMFS 支持至少 128 个文件，单文件最大 64KB
- FAT32 支持实机磁盘文件持久化存储
- 目录层级至少支持 3 层嵌套
- 文件读写支持 seek 操作
- 提供 mkfs 工具初始化文件系统镜像

### 6. 磁盘驱动（SATA AHCI）

**⚠️ 实机磁盘是 SATA AHCI 模式，不是 IDE PIO。需要实现 AHCI 规范。**

**功能示例：**
- 理解 AHCI 1.x 规范：HBA 内存映射寄存器、Port 寄存器、Command List、FIS 结构
- PCI 枚举：通过 PCI 总线扫描找到 AHCI 控制器（Class 0x01, Subclass 0x06）
- 初始化 AHCI HBA：复位控制器、配置中断、启动各 Port
- 实现 FIS-Based PIO 模式（AHCI 简化路径，先不做 DMA）
- 实现 ATA IDENTIFY 命令，识别磁盘型号和参数
- 实现 ATA READ/WRITE DMA48（LBA48 寻址，支持 >128GB 磁盘）
- 文件系统层对接：提供块设备读写接口

**技术指标：**
- 成功通过 AHCI 读取磁盘 ATA IDENTIFY 数据（型号、序列号、容量）
- 磁盘读写速度达到 PIO 模式理论值的 50% 以上（~2MB/s）
- 支持 LBA48 寻址（111.8GB 磁盘全部可访问）
- PCI Class Code 0x010601 设备成功枚举
- ABAR（AHCI Base Address Register）正确映射

**开发建议：**
由于 AHCI 规范复杂（~100 页），建议分步：
1. 先在 QEMU 上用 `-device ahci` 模拟 AHCI 控制器验证逻辑
2. 确认命令流程正确后，再上实机测试
3. 实机的 Intel Sunrise Point-LP AHCI 和 QEMU 的模拟 AHCI 寄存器级兼容

### 7. 显示驱动（VGA / VESA）

**实机显卡：Intel UHD Graphics 620（集显）+ NVIDIA MX130（独显）。开发用 VGA 兼容模式，暂不涉及 GPU 厂商特定驱动。**

**功能示例：**
- VGA 文本模式（80×25，端口 0x3D4/0x3D5，显存段 0xB8000）
- 实现基本的 printk 内核日志输出到 VGA
- 支持光标控制、颜色、清屏
- 可选：探测 VESA/VBE BIOS 扩展，获取显卡支持的图形模式列表
- 可选：设置 VESA 图形模式（1024×768 或 800×600），实现简单像素绘制

**技术指标：**
- VGA 文本输出延迟 <1ms/字符
- printk 支持格式字符串（%s, %d, %x）
- 支持滚动（80×25 满屏后上滚）
- VESA 模式（如果实现）：至少 1024×768×24bpp

### 8. 键盘驱动（PS/2）

**实机键盘：PS/2 AT Translated Set 2，端口 0x60/0x64，IRQ 1。硬件已确认。**

**功能示例：**
- 初始化 PS/2 控制器（端口 0x60 数据，0x64 命令/状态）
- 实现 IRQ 1 中断处理程序，读取扫描码
- 扫描码 → ASCII 转换（支持基本 US QWERTY 布局）
- 处理组合键：Shift、Ctrl、Caps Lock
- 实现键盘缓冲区（环形队列），解耦中断处理和用户态读取
- 通过系统调用向用户程序暴露键盘输入

**技术指标：**
- PS/2 按键响应延迟 <50ms（中断到用户可读取）
- 支持所有字母数字键 + 常用符号（ASCII 可打印字符 + 回车/Tab/退格）
- 支持 Shift 切换大小写和符号
- 缓冲区至少 256 字节

### 9. 定时器（PIT 8253/8254）

**功能示例：**
- 初始化 PIT 8253/8254（端口 0x40-0x43），设置时钟中断频率
- 实现 IRQ 0 时钟中断处理：tick 计数器递增
- 提供 msleep/wakeup 接口，进程可等待定时唤醒
- 实现 jiffies 系统全局时钟计数

**技术指标：**
- 时钟中断频率可配置（默认 100Hz，即每 10ms 一次）
- 实机频率误差 <1%
- 支持毫秒级定时（msleep 精度 ±10ms）

### 10. 中断控制器（8259A PIC）

**功能示例：**
- 8259A PIC 初始化与重映射（主片 0x20-0x21，从片 0xA0-0xA1）
- IRQ 重映射：主片 IRQ0-7 → 中断向量 0x20-0x27，从片 IRQ8-15 → 0x28-0x2F
- 实现 EOI（End of Interrupt）机制，中断处理完后通知 PIC
- 实现 IRQ 屏蔽/解除屏蔽

**技术指标：**
- 支持 16 个硬件中断源（IRQ 0-15）
- 中断响应延迟 <100μs
- 支持嵌套中断（IRQ 优先级）

### 11. 用户程序加载与执行

**功能示例：**

- 实现 ELF 格式可执行文件的解析与加载
- 实现用户态栈初始化（参数传递、环境变量）
- 实现系统调用封装库（libc 简化版）
- 实现 shell 命令行解释器，支持基本命令：
    - `ls`：列出目录内容
    - `cat`：显示文件内容
    - `echo`：输出字符串
    - `ps`：显示进程列表，包含 CPU 和内存使用
    - `kill`：发送信号终止进程
    - `exec`：执行外部程序
    - `uname`：显示系统信息（内核版本、内存总量、运行时间）
    - `reboot`：通过键盘控制器重启实机

**技术指标：**

- 至少能加载运行 5 个不同的用户程序
- shell 支持命令解析、参数传递、管道
- 用户程序崩溃不影响内核稳定性

### 12. 可启动镜像制作与部署（新增）

**功能示例：**

- 编写 Makefile 构建系统：
    - 编译 bootloader（nasm 汇编）
    - 编译内核（gcc C 代码）
    - 链接生成 ELF 内核镜像
    - 生成可启动的磁盘镜像（raw 格式）
- 制作 FAT32 格式的可启动 U 盘镜像
- 支持两种部署方式：
    1. **U 盘启动**：`dd` 将镜像写入 U 盘，BIOS 从 USB-HDD 引导
    2. **硬盘启动**：`dd` 将镜像写入笔记本硬盘 MBR，直接引导
- 提供自动化构建与烧录脚本（`make && make install`）

**技术指标：**

- 一键构建：`make` 生成可启动镜像
- 一键烧录：`make write-usb` 写入指定 U 盘
- 镜像大小 ≤ 128MB（适合老旧 U 盘和硬盘容量）
- 构建过程在 Linux 开发机上完成，交叉编译工具链：i686-elf-gcc + nasm

## 三、开发与测试流程

### 方式 A：直接实机开发（全实机）

```
┌─────────────────┐     ┌─────────────────┐
│  编写/修改代码   │ ──▶ │  make && 烧录U盘 │
│  (开发机)       │     │  & 实机重启测试  │
└─────────────────┘     └─────────────────┘
        ▲                       │
        └── 根据实机反馈修复 ◀──┘
```

- **日常开发**：编译 → 烧录 U 盘 → 笔记本重启 → 观察行为
- **优点**：驱动代码一步到位，无需 QEMU 适配层
- **缺点**：迭代周期较慢（每次改代码都要重新烧录+重启）

### 方式 B：QEMU 辅助 + 实机验证（模拟器优先）

```
┌─────────────────┐     ┌──────────────────┐     ┌─────────────────┐
│  编写/修改代码   │ ──▶ │ QEMU 快速验证     │ ──▶ │  实机烧录测试    │
│  (开发机)       │     │ (make qemu)      │     │ (make write-usb) │
└─────────────────┘     └──────────────────┘     └─────────────────┘
        ▲                                                 │
        └────────────── 修复 Bug ◀────────────────────────┘
```

- **日常开发**：QEMU + GDB 远程调试，秒级启动
- **优点**：2 秒看到效果，支持断点/单步/看内存
- **缺点**：QEMU 对硬件的模拟不完全精确，最终仍需实机适配

### 推荐策略

根据开发阶段灵活切换：

| 阶段 | 推荐方式 | 原因 |
|------|----------|------|
| bootloader + 保护模式切换 | QEMU + 实机混用 | 这部分复杂性高，QEMU 能查看寄存器/内存状态 |
| 中断/PIC/键盘 | QEMU + 实机混用 | 逻辑简单，可以直上实机 |
| 内存管理 | QEMU 为主 | 页表/缺页异常的调试需要 GDB 级别观察 |
| 磁盘 ATA 驱动 | **实机为主** | QEMU 的 ATA 模拟和真实硬件差别大，直接实机写 |
| 进程调度 | QEMU 为主 | 需要精确的时钟中断配合 |
| 文件系统 + Shell | 随意 | 逻辑层不依赖硬件，两种方式皆可 |

**最终验收**：老笔记本原生运行，完成所有功能演示

## 四、里程碑

| 阶段 | 内容 | 验证方式 |
|------|------|----------|
| M1 | bootloader + VGA 输出 "Hello World" | QEMU + 实机 |
| M2 | 中断 + 键盘输入 + 时钟 | QEMU + 实机 |
| M3 | 内存管理（物理 + 虚拟） | QEMU |
| M4 | 进程管理 + 调度 | QEMU |
| M5 | 文件系统 + ATA 磁盘读写 | QEMU |
| M6 | 用户程序 + Shell | QEMU |
| M7 | 系统集成 + 实机适配调试 | 实机 |
| M8 | 最终验收：实机原生运行演示 | 老笔记本 |
